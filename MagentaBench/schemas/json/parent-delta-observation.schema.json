{
  "$defs": {
    "ResearchMetricDisposition": {
      "description": "Disposition of one unit in a pre-declared research population.",
      "enum": [
        "observed",
        "zero_filled",
        "excluded",
        "missing",
        "invalid",
        "censored"
      ],
      "title": "ResearchMetricDisposition",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "properties": {
    "child_score": {
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Child Score"
    },
    "disposition": {
      "$ref": "#/$defs/ResearchMetricDisposition"
    },
    "pair_id": {
      "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9._-]*[A-Za-z0-9])?$",
      "title": "Pair Id",
      "type": "string"
    },
    "parent_score": {
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Parent Score"
    },
    "reason": {
      "anyOf": [
        {
          "minLength": 1,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Reason"
    }
  },
  "required": [
    "pair_id",
    "disposition"
  ],
  "title": "ParentDeltaObservation",
  "type": "object"
}
