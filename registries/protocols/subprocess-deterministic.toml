[protocol]
id = "subprocess.deterministic.v1"
kind = "mechanism_validation"
adapter = "magentabench.scheduler"
bmp_version = "0.1"
rollouts_per_case = 1
parallelism = 1
case_order = "seeded_random"
adaptive_budget = false
candidate_selection = "exact"
state_reset = "per_rollout"
checkpoint_policy = "save_and_resume"
deterministic_conformance = false

[protocol.budget]
max_tokens = 0
max_wall_seconds = 1.0
max_cost = 0.0
